programming_words = {
    'list': 'List are collections of related data/values.',
    'loop': 'Loops are ways to do task repeatively.',
    'dictionary': 'Dictionaries are collections of key and value pairs.',
    'variable': 'Variables are containers that store data.',
    'tuple': 'Tuples are unique set of data that cannot be changed.'
}

print('\nList:', programming_words.get('list'))

print('\nLoop:', programming_words.get('loop'))

print('\nDictionary:', programming_words.get('dictionary'))

print('\nVariable:', programming_words.get('variable'))

print('\nTuple:', programming_words.get('tuple'))
